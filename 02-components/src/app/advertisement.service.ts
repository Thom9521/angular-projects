export class AdvertisementService {
  getAdvertisements(): string[] {
    return [
      "Angular is a framework for large projects",
      "TypeScript brings in types",
      "JavaScript is the programming language of the web"
    ];
  }
}
